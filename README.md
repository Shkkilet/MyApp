8 завдання
Якщо додати ToList після Where, то фільтрування одразу виконується і query буде уже списком і при додаванні чисел до масиву numbers не повпливає на результат query.
Додаткові завдання 
1.
GetAll() IEnumerable повністю не захищає через те, що просто передає той самий лист але просто через інтерфейс. Щоб захистити повератити через метод просто копію нашого листа. Також можна використати IReadOnlyCollection це буде гарантувати , що зовнішній код не зможе змінитит наш список.
2.IEnumerable працює із даними в пам'яті , тобто фільтрація виконується кодом c#. IQueryable для роботи із звовнішніми даними такі як бд. Тобто працює із sql і переводиться у sql запити.

3. Dictionary кращий тоді уоли потрібно шукати за унікальним ключем в разі цього оцінка складності буде O(1) на відміну від List в якому пошук буде лінійним і складність буде O(n) , якщо ми будемо шукати не по індексу. При пошуку по індексу складність теж O(1). Також List корисним тим , що він зберігає порядок даних.
